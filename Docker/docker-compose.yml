version: '3.8'
services:
  dokuwiki:
    image: 'docker.io/bitnami/dokuwiki:20200729'
    restart: unless-stopped
    ports:
      - '8080:80'
      - '8443:443'
    environment:
      DOKUWIKI_USERNAME: admin
      DOKUWIKI_PASSWORD: admin
    volumes:
      - $PWD/dokuwiki_data:/bitnami/dokuwiki
volumes:
  dokuwiki_data:
    driver: local
